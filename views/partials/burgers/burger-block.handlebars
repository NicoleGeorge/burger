<li>
	{{ name }}
	<button class="devour-burger  {{#if devour}} green {{else}} red {{/if}}" data-id="{{ id }}" data-newburger="{{ devourBurger }}">
		{{#if devour}}
			DEVOURED!
		{{else}}
			DEVOUR! 
		{{/if}}
	</button>
	{{!-- <button class="delete-burger" data-id="{{this.id}}">YUCK!</button> --}}
</li>
